@model List<ApplicationCore.Models.FavoriteRequestModel>

@{
    ViewData["Title"] = "Favorites";
}

<div class="rounded">
    <div class="container-fluid">
        <div class="row">
            @foreach (var favorite in Model)
            {
                <div class="col-xl-2 col-sm-4 col-lg-3 col-6">
                    <div class="card mb-4">
                        <a asp-controller="Movies" asp-action="Details" asp-route-id="@favorite.MovieId" >
                            <img src="@favorite.PosterUrl" class="img-fluid" alt="@favorite.MovieId" />
                        </a>
                    </div>

                </div>
            }
        </div>
    </div>
</div>
